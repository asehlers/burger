<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}EAT THE BURGER!{{else}}MAKE IT AGAIN!{{/if}}
	</button>
	{{!-- <button class="delete-burger" data-id="{{id}}">DELETE burger!</button> --}}
</li>
